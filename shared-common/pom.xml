<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://apache.org"
         xmlns:xsi="http://w3.org"
         xsi:schemaLocation="http://apache.org https://apache.org">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.tmobile</groupId>
        <artifactId>tmobile-order-system</artifactId>
        <version>1.0.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>shared-common</artifactId>
    <version>1.0.0</version>
    <name>shared-common</name>
    <description>Shared Data Contracts and Libraries for T-Mobile Services</description>

    <!-- Note: No spring-boot-maven-plugin here because this is a library, not an app! -->
</project>
