# Network configuration
server.port=8011

# Application naming
spring.application.name=product-service

# MongoDB connection string (creates 'product_db' automatically if it doesn't exist)
spring.data.mongodb.uri=mongodb://localhost:27017/product_db
